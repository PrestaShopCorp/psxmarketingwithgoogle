enum CampaignStatus {
  NOTELIGIBLE = 'NOT ELIGIBLE',
  ELIGIBLE = 'ELIGIBLE',
  PAUSED = 'PAUSED',
  REMOVED = 'REMOVED',
  PENDING = 'PENDING',
  ENDED = 'ENDED',
}

export enum CampaignStatusToggle {
  ENABLED = 'ENABLED',
  PAUSED = 'PAUSED',
}

export default CampaignStatus;

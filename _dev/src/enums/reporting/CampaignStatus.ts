enum CampaignStatus {
  NOTELIGIBLE = 'NOT ELIGIBLE',
  ELIGIBLE = 'ELIGIBLE',
  PAUSED = 'PAUSED',
  REMOVED = 'REMOVED',
  PENDING = 'PENDING',
  ENDED = 'ENDED',
  ENABLED = 'ENABLED',
}

export default CampaignStatus;
